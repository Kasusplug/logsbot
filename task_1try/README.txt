В данном проекте я хочу разработать пайтон бота с помощью библиотеки telebot

Задача - 
1 - написать на самом python парсер логов объектно ориентированно
2 - написать парсер конкретно под кокнертную задачу

    1) по логам в задаче делаю вывод что:
        - они делятся на 3 части(INFO, ERROR, DEBUG)
        - нужно будет выводить все(возможно по временным отрезкам, тк их довольно много)

3 - так же хотелось бы подвести статистику в какую минуту/час сколько логов с каким ответом приходило

4 - заняться уже самим ботом с тем же функционалом:

    1) Забираем логи
    2) Анализируем
    3) Даем ответ по описанному выше подходу